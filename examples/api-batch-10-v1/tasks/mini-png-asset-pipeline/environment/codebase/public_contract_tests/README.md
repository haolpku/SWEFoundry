Public smoke tests for mini-png-asset-pipeline.

Run each file with Python from any working directory. Each smoke test inserts the surrounding environment/codebase directory into sys.path before importing png_asset_pipeline. These tests are intentionally small public contract checks; the private step verifiers exercise the full behavioral contract disclosed in each step instruction.
