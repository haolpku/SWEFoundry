Public contract smoke tests for mini-ical-sync-engine.

Run these files against a candidate codebase by placing environment/codebase on sys.path. The progressive hidden behavioral names disclosed by the contract are: line unfolding, parameter parsing, canonical UID ordering, bound inclusivity, RRULE interval handling, exception date semantics, tombstone precedence, sequence comparison, change ordering independence, tie-break determinism, recurrence override conflicts, tombstone stability, steps 1-4 regression, snapshot migration, and tombstone compaction safety.
